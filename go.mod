// The docs repo is pure Markdown, carried as a Hugo Module (Go module) so the
// website can mount it under /docs/. It has no Go code and no presentation; all
// styling and site configuration live in the website repo (ADR 0011).
module github.com/valiss-dev/docs

go 1.26
