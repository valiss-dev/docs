name: Test

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v7

    - name: Check Markdown formatting
      uses: dprint/check@v2.3
      with:
        dprint-version: 0.55.2
